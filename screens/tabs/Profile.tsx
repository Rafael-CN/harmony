import { Text } from "react-native";
import { Background, Span } from "../../components";

export default function Profile() {
    return (
        <Background>
            <Span>Profile</Span>
        </Background>
    );
}
